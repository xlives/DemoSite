<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="
        http://www.springframework.org/schema/jee
        http://www.springframework.org/schema/jee/spring-jee-3.2.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.2.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop-3.2.xsd">


    <bean id="blCheckoutWorkflow" class="org.broadleafcommerce.core.workflow.SequenceProcessor">
        <property name="activities">
            <list>
                <!-- This activity should occur at the very end of the checkout workflow, after everything has been executed -->
                <bean p:order="9999999" class="com.mycompany.worklow.checkout.SendOrderConfirmationEmailActivity" />
            </list>
        </property>
    </bean>

    <!--Only complete the order when in preview mode, and don't execute the rest of the activities in the checkout
    workflow-->

    <bean id="blPreviewCheckoutWorkflow" class="org.broadleafcommerce.core.workflow.SequenceProcessor">
        <property name="processContextFactory">
            <bean class="org.broadleafcommerce.core.checkout.service.workflow.CheckoutProcessContextFactory"/>
        </property>
        <property name="activities">
            <list>
                <ref bean="blCompleteOrderActivity" />
            </list>
        </property>
        <property name="defaultErrorHandler" ref="blDefaultErrorHandler"/>
    </bean>

    <bean id="blPreviewAwareCheckout" class="com.broadleafcommerce.enterprise.workflow.site.service.workflow.PreviewAwareAOP">
        <property name="previewWorkflowOrActivity" ref="blPreviewCheckoutWorkflow"/>
    </bean>

    <aop:config>
        <aop:aspect id="previewCheckoutAspect" ref="blPreviewAwareCheckout">
            <aop:pointcut id="previewCheckoutMethod" expression="execution(* org.broadleafcommerce.core.workflow.Processor.doActivities(..)) and bean(blCheckoutWorkflow)"/>
            <aop:around method="doActivities" pointcut-ref="previewCheckoutMethod"/>
        </aop:aspect>
    </aop:config>

    <!--For Enterprise sandbox preview, there may be one or more activities that should execute alternate logic when in preview mode,
    as opposed to production.-->

    <bean id="previewCompleteActivity" class="com.broadleafcommerce.enterprise.workflow.site.service.workflow.PreviewCompleteOrderActivity"/>

    <bean id="blPreviewAwareCompleteOrder" class="com.broadleafcommerce.enterprise.workflow.site.service.workflow.PreviewAwareAOP">
        <property name="previewWorkflowOrActivity" ref="previewCompleteActivity"/>
    </bean>

    <aop:config>
        <aop:aspect id="previewCompleteOrderAspect" ref="blPreviewAwareCompleteOrder">
            <aop:pointcut id="previewCompleteOrderMethod" expression="execution(* org.broadleafcommerce.core.workflow.Activity.execute(..)) and bean(blCompleteOrderActivity)"/>
            <aop:around method="doActivities" pointcut-ref="previewCompleteOrderMethod"/>
        </aop:aspect>
    </aop:config>

</beans>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="
        http://www.springframework.org/schema/jee
        http://www.springframework.org/schema/jee/spring-jee-3.2.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.2.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop-3.2.xsd">


    <bean id="blCheckoutWorkflow" class="org.broadleafcommerce.core.workflow.SequenceProcessor">
        <property name="activities">
            <list>
                <!-- This activity should occur at the very end of the checkout workflow, after everything has been executed -->
                <bean p:order="9999999" class="com.mycompany.worklow.checkout.SendOrderConfirmationEmailActivity" />
            </list>
        </property>
    </bean>

    <!--For Enterprise sandbox preview, there may be one or more activities that should execute alternate logic when in preview mode,
    as opposed to production. For example, if you're previewing sandbox changes for a site, you probably don't want to
    make a real credit card charge during checkout. This would also apply to any fulfillment activity, as you would not
    want to actually ship product for a preview order. The use of PreviewAwareActivity allows you to accomplish this by
    specifying two separate activities (a preview version and a production version) for a particular discrete activity
    operation (e.g. payment, as seen below).-->

    <bean id="demoPaymentActivity" class="org.broadleafcommerce.core.payment.service.workflow.CompositeActivity">
        <property name="workflow" ref="blAuthorizeAndDebitWorkflow"/>
    </bean>

    <!--by default, use the same payment activity for preview and prod for the heatclinic demo-->
    <bean p:order="1000" id="blPaymentActivity" class="com.broadleafcommerce.enterprise.workflow.site.service.workflow.PreviewAwareActivity">
        <property name="previewActivity" ref="demoPaymentActivity"/>
        <property name="productionActivity" ref="demoPaymentActivity"/>
    </bean>

</beans>